# This file makes the "scoring" folder a Python PACKAGE.
#
# JAVA PARALLEL:
#   In Java, packages are just directories with classes in them — no special file needed.
#   In Python, a directory only becomes an importable package if it contains __init__.py.
#   (In modern Python 3.3+, this is technically optional for "namespace packages,"
#    but it's still best practice to include it.)
#
#   Think of this file as: "Dear Python, this folder contains importable modules."
