server.port=8080

# thresholds
sentinel.threshold.risk-score=0.70
sentinel.threshold.ring-score=0.60

# python agent host
sentinel.python-agent.base-url=http://localhost:8000

# default cost config
sentinel.cost.currency=USD
sentinel.cost.per-false-positive=50.0
